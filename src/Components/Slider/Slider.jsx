import { useState } from "react"

export function Slider() {

    const [sliderIndex, setSliderIndex] = useState(0);
    
    

    function forward(){
        if sliderIndex >= 
    }
    
    return (

    )
}